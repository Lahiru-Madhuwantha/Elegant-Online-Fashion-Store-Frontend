import React from "react";
import styled from "styled-components";

function Card(){
    return(
        <Container>
            <h1>Card</h1>
        </Container>
    );
}

const Container = styled.div`

`;

export default Card;